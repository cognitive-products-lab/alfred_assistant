# Visual Studio
.vs/

# Logs
logs/

# Backups
_BACKUPS/

# Python cache
__pycache__/
*.pyc

# Virtual env
venv/
.venv/
.env/
.env

# Databases
*.db
*.sqlite3

# Secrets / clés cryptographiques
*.key
*.pem
*.p12

# Temp
temp/
tmp/

# OS
Thumbs.db
.DS_Store

*.wav
output.wav
output_python.wav
*.onnx

# =========================
# ALFRED - Tests & Debug
# =========================

.pytest_tmp/
.pytest_cache/

# Debug
*_debug.txt
debug_*.txt

# Sessions mémoire dynamiques
data/memory/sessions/

# Rapports temporaires
htmlcov/
.coverage

# Artefacts de tests sécurité (régénérés à chaque run)
backup/security/*.bak

# Verrous locaux Claude Code
src/security/.claude/*.lock

# Sorties de scripts de test ad-hoc
test_run_output.txt

# Claude Code internal (worktrees, sessions)
.claude/

# =========================
# SÉCURITÉ — NE JAMAIS COMMITER
# =========================

# Profil psychologique — données sensibles
data/profile/answers/
data/profile/answers_encrypted/
data/profile/keys/
*.fernet_key
user_answers_*.json

# Profils onboarding (personnalité/émotionnel/santé) — données personnelles/santé sensibles
data/profile/personality_*.json
data/profile/emotional_*.json
data/profile/identity_*.json
data/profile/persona_private_*.json
data/profile/user_profile.json
data/health/

# Mémoire personnelle (conversations réelles, suivi santé/bien-être, épisodes) — données personnelles sensibles
# Exclues le 19/07/2026, backup dans G:\BACKUP_ALFRED\FICHIERS_SENSIBLES_EXCLUS_GIT\2026-07-19\
data/memory/episodic/dialogue_history.json
data/memory/wellbeing_log.json
data/memory/episodes.json

# Rapports d'audit/scan horodates (regeneres 2x/jour) — logs jetables, pas de la donnee source
# (dashboard_*.json fait deja foi ; ces rapports gonfleraient l'historique indefiniment)
dashboard/dashboard_conformite/reports/
dashboard/dashboard_gouvernance/reports/
dashboard/dashboard_risk_impact/reports/
dashboard/dashboard_vulnerabilites/reports/

# Sauvegardes de secrets (backups de fichiers déjà exclus ci-dessus)
data/security/*.bak_*

# Clé de chiffrement Fernet
data/security/fernet.key

# Clé privée TLS locale (companion API, B24) — le certificat public .crt reste versionné
data/security/certs/companion_api/server.key

# Hashes PIN utilisateurs (B05 — jamais transmettre)
data/security/pins.json

# Secrets MFA, appareils de confiance, journaux de sécurité (purgés de l'historique le 08/07/2026)
data/security/mfa_secrets.json
data/security/trusted_devices.json
data/security/trusted_devices_runtime.json
data/security/incident_register.json
data/security/access_decisions_history.json
data/security/behavior_baseline.json
data/security/failed_attempts.json
data/security/quarantine_registry.json
data/security/pending_approvals.json

# Documents privés / confidentiels
docs/entretien/

# PoC Hadoop (Bloc B29) — données anonymisées régénérables, pas de source
data/hadoop_poc/

# Identifiants OAuth Google Cloud + jeton d'accès chiffré (Chantier 1 — Agenda)
auth/
data/security/google_calendar_token.json

# Jeton d'accès chiffré Google Home / Nest (Chantier 1B — Device Access)
data/security/google_home_token.json
