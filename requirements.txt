# ============================================================
# ALFRED — requirements.txt consolidé
# Version : Foundation (V1 → V4 ready)
# Principe : local-first, security by design, zero trust
# ============================================================
# Installer avec :
#   pip install -r requirements.txt
# ============================================================

# --- CORE ---
python-dotenv>=1.0.0        # Gestion des secrets (.env)
pydantic>=2.0.0             # Validation et structuration des données

# --- SÉCURITÉ (Bloc 20) ---
cryptography>=42.0.0        # Chiffrement Fernet (AES-128)
bcrypt>=4.0.0               # Hachage sécurisé (PIN, mot de passe)
PyJWT>=2.8.0                # Tokens JWT pour sessions

# --- API COMPAGNON (Bloc 24 — interface/companion_api.py, PoC ALFRED_ANDROID) ---
fastapi>=0.110.0             # API locale consommée par le client Android
uvicorn>=0.29.0               # Serveur ASGI pour interface/companion_api.py

# --- NLP / IA LOCALE ---
# Choisir selon le modèle local utilisé :
# llama-cpp-python>=0.2.0   # LLM local (Llama, Mistral, etc.)
# transformers>=4.40.0      # HuggingFace (modèles locaux)
# torch>=2.0.0              # PyTorch (requis par transformers)
sentence-transformers>=5.0.0  # Embeddings pour RAG (actif depuis le 18/08/2026, session 6)

# --- SPEECH (STT / TTS) ---
# whisper>=1.1.4            # STT offline (OpenAI Whisper local)
# pyttsx3>=2.90             # TTS offline basique (V1)
# TTS>=0.22.0               # Coqui TTS / Piper (V2+, qualité)

# --- MÉMOIRE / BASE DE DONNÉES ---
# sqlite3 est inclus dans Python — pas d'installation nécessaire
chromadb>=1.5.0             # Vector DB locale (mémoire sémantique, actif depuis le 18/08/2026, session 6)
# faiss-cpu>=1.8.0          # Recherche vectorielle locale (alternative non utilisée)

# --- GOUVERNANCE DATA (Bloc 11.05 — scan_data_assets.py) ---
# Lecture seule des bases PostgreSQL/MongoDB d'ALFRED_WEB pour le scanner
# d'actifs de données (pilier Automatisation du framework DQbD, mémoire M2).
psycopg2-binary>=2.9.0       # Introspection PostgreSQL (information_schema)
pymongo>=4.7.0                # Introspection MongoDB (list_collection_names)

# --- INTERFACE (GUI) ---
pywebview>=6.2.0             # Fenêtre desktop native pour interface/desktop_ui (src/alfred_desktop.py)
# kivy>=2.3.0               # Ancienne interface Kivy (src/alfred_with_ui.py, conservée en fallback)
# kivymd>=1.2.0             # Material Design pour Kivy

# --- GOOGLE (Chantier 1 — Agenda) ---
google-auth-oauthlib>=1.2.0   # Flux OAuth desktop (src/integrations/google_auth.py)
google-auth-httplib2>=0.2.0   # Transport HTTP pour les appels authentifiés
google-api-python-client>=2.130.0  # Client Google Calendar API

# --- OUTLOOK (Agenda, second fournisseur — 24/07/2026) ---
msal>=1.28.0                  # Flux OAuth desktop Microsoft (src/integrations/outlook_auth.py)
requests>=2.31.0              # Appels REST Microsoft Graph (src/integrations/outlook_calendar_client.py)

# --- DOMOTIQUE (V4) ---
# paho-mqtt>=2.0.0          # MQTT pour objets connectés
# requests déjà listé plus haut (section OUTLOOK) — aussi utilisable pour météo/Google Home

# --- AUDIT / SÉCURITÉ DEV ---
bandit>=1.7.0               # Scan sécurité du code Python
pip-audit>=2.7.0            # Audit des dépendances

# --- TESTS ---
pytest>=8.0.0               # Framework de tests
pytest-cov>=5.0.0           # Coverage des tests

# --- UTILITAIRES ---
pathlib2>=2.3.7             # Gestion des chemins (compat)
python-dateutil>=2.9.0      # Manipulation des dates
tzdata>=2024.1               # Base IANA des fuseaux horaires (zoneinfo, requis sur Windows pour "Europe/Paris")

# ============================================================
# NOTES D'INSTALLATION
# ============================================================
# 1. Toujours travailler dans le venv :
#    python -m venv .venv
#    .venv\Scripts\Activate.ps1  (Windows)
#
# 2. Décommenter les lignes selon la phase active :
#    V1 : sections CORE + SÉCURITÉ + TESTS
#    V2 : + NLP local (modèle léger)
#    V3 : + STT/TTS + mémoire sémantique
#    V4 : + domotique
#
# 3. Ne jamais installer en global (hors venv)
# ============================================================
